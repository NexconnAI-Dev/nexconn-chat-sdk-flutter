/// Represents the push notification type for Vivo devices.
enum VivoPushType {
  /// Operational push notification (e.g., promotions, marketing).
  operate,

  /// System push notification (e.g., alerts, critical updates).
  system,
}
