/// Represents the type of friend request application relative to the current user.
enum FriendApplicationType {
  /// The friend request was sent by the current user.
  sent,

  /// The friend request was received by the current user.
  received,
}
