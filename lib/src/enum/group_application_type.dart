/// Represents the type of a group membership application.
enum GroupApplicationType {
  /// A user applied to join the group.
  application,

  /// A group member invited a user to join the group.
  invitation,
}
