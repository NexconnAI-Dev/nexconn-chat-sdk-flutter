/// Represents the importance level for Huawei push notifications.
enum HuaweiPushImportance {
  /// Normal importance level (default notification behavior).
  normal,

  /// Low importance level (silent notification).
  low,
}
