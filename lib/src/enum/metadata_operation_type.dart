/// Represents the type of operation performed on metadata (key-value storage).
enum OpenChannelMetadataOperationType {
  /// Update or add a metadata entry.
  update,

  /// Remove a metadata entry.
  remove,
}
