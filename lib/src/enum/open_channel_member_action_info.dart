/// Represents the type of participant action in an open channel.
enum OpenChannelParticipantActionType {
  /// Unknown or unspecified action.
  unknown,

  /// A participant joined the open channel.
  join,

  /// A participant left the open channel.
  leave,
}
